Basic PeriodComment:

```jsx
let period = {
    duration: 'period',
    per_id: 42,
    per_comment: 'Test Comment',
    type: {
        pty_config: {
            icon: 'fa-question',
            bgcolor: 'green',
            color: 'black',
        },
        pty_name: 'my Title',
    },
};

<PeriodComment period={period} />
```
