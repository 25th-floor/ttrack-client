Basic DatePickerButton:

```jsx
const {BrowserRouter} = require('react-router-dom');

<div>
    <BrowserRouter>
        <DatePickerButton className="myClass" link="myLink">Button</DatePickerButton>
    </BrowserRouter>
    <div style={{ clear: 'both' }} />
</div>
```
