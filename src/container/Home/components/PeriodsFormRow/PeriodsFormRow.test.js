import React from 'react';
import { shallow } from 'enzyme';
import PeriodsFormRow from './PeriodsFormRow';


describe('<PeriodsFormRow />', () => {
    it('Expect to have unit tests specified', () => {
        expect(true).toEqual(true);
    });
});
