// @flow

import { config } from '../Constants';
import type { ApiVacationsType } from './ResourcesTypes';

const { client } = config;

export const Vacations = {
    collection: async (start: number = 0): Promise<ApiVacationsType> => client.get(`vacations?start=${start}`),
};
