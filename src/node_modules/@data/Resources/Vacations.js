// @flow

import { config } from '../Constants';
import type { ApiVacationsType } from './ResourcesTypes';

const { client } = config;

export const Vacations = {
    collection: (start = 0): ApiVacationsType => client.get(`vacations?start=${start}`),
};
