import actionTypes from './TimesheetActionTypes';

const initialState = {
    month: {},
};

export const TimesheetReducer = (state = initialState, action) => {
    switch (action.type) {
        case actionTypes.RECEIVE: {
            return state;
        }

        default:
            return state;
    }
};
