// @flow

import actionTypes, {
    type ReloadActionType,
} from './MottoTypes';

export function reload(): ReloadActionType {
    return {
        type: actionTypes.RELOAD,
    };
}

export default {
    reload,
};
