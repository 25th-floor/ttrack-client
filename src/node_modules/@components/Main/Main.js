import React from 'react';
import './Main.css';

/**
 * Main
 */
const Main = ({ className = 'col mx-auto', children }) => (
    <div className={className}>
        {children}
    </div>
);

export default Main;
