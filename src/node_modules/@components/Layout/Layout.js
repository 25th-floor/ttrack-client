import React from 'react';
import './Layout.css';

/**
 * Layout
 */
export const Layout = ({ children }) => (<div className="container-fluid p-0">
    <div className="row m-3">
        {children}
    </div>
</div>);

export default Layout;
