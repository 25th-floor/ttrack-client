import React from 'react';
import { Motto, TTLogo } from '@components';

const styles = {
    brandContainer: {
        fontWeight: '100',
    },
};

/**
 * Brand
 */
export const Brand = () => (
    <div className="brand-container" style={styles.brandContainer}>
        <TTLogo />
        <span className="brand-title" style={styles.brandTitle}>
            <strong>Time</strong> Tracking
        </span>

        <small className="brand-motto">
            <Motto />
        </small>
    </div>
);

export default Brand;
